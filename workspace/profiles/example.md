---
name: "example"
description: "Example profile — template for creating new profiles"
allowed_tools:
  - "filesystem.read"
  - "http.get"
mcp_servers: []
---
# Example Profile — Template

This is a template profile. Copy this file to create a new profile:

1. Rename the file (e.g., `my-profile.md`)
2. Update the `name` and `description` in frontmatter
3. List the tools your profile needs in `allowed_tools`
4. Add MCP server configs under `mcp_servers` (or leave empty)
5. Write your system prompt below this frontmatter

You are an example assistant. Replace this text with your actual system prompt.
